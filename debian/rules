#!/usr/bin/make -f

export PYBUILD_NAME=truenas-pydiscovery
export PYBUILD_SYSTEM=pyproject
export PYBUILD_DISABLE=test

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_installsystemd:
	dh_installsystemd --name=truenas-discoveryd
